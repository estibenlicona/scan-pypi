# ==================# -------------------------
# CACHE CONFIGURATION (Optional)
# -------------------------
# Enable/disable caching (true/false, default: true)
CACHE_ENABLED=true

# Cache directory path (default: .cache)
CACHE_DIRECTORY=.cache

# Cache TTL in hours (default: 24)
CACHE_TTL_HOURS=24

# -------------------------
# API CONFIGURATION (Optional)
# -------------------------
# HTTP request timeout in seconds (default: 30)
API_TIMEOUT_SECONDS=30

# Maximum number of concurrent requests (default: 10)
API_MAX_CONCURRENT_REQUESTS=10

# Request delay between calls in milliseconds (default: 100)
API_REQUEST_DELAY_MS=100

# -------------------------
# LOGGING CONFIGURATION (Optional)
# -------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOGGING_LEVEL=INFO

# Log format: standard or json (default: standard)
LOGGING_FORMAT_TYPE=standard

# -------------------------
# REPORT CONFIGURATION (Optional)
# -------------------------
# Output directory for reports (default: reports)
REPORT_OUTPUT_DIR=reports

# Report filename template (default: analysis_report_{timestamp}.json)
REPORT_FILENAME_TEMPLATE=analysis_report_{timestamp}.json

# Include detailed dependency tree (true/false, default: true)
REPORT_INCLUDE_DEPENDENCY_TREE=true

# =========================================
# EXAMPLE USAGE AFTER CONFIGURATION:
# =========================================
# export $(cat .env | xargs) && python -m src.interface.cli.main requests flask
# Or in PowerShell: Get-Content .env | ForEach-Object { $name, $value = $_.split('='); [Environment]::SetEnvironmentVariable($name, $value) }===
# PYPI SCANNER - Environment Configuration
# =========================================
# Copy this file to .env and fill in your values
# This follows Clean Code principle: "usa .env para configuración"

# -------------------------
# SNYK CONFIGURATION (Required)
# -------------------------
# Organization ID from your Snyk account
# You can find this in your Snyk dashboard > Settings > Organization
SNYK_ORG=your-organization-name

# Snyk API Token for authentication
# Get from: https://app.snyk.io/account (Account Settings > API Token)
SNYK_TOKEN=your-snyk-token-here

# Snyk CLI timeout in seconds (optional, default: 60)
SNYK_TIMEOUT=60

# Directorio donde se almacenan los archivos de caché
# Ruta relativa al directorio raíz del proyecto
CACHE_DIR=.cache

# Tiempo de vida del caché en horas
# Después de este tiempo, los archivos de caché expiran y se regeneran
CACHE_TTL_HOURS=24

# ================================
# CONFIGURACIÓN DE SNYK
# ================================

# Organización de Snyk (requerido para análisis)
SNYK_ORG=tu_organizacion_snyk

# Carpeta que contiene los archivos de entrada para el escaneo
SCAN_FOLDER=scan_input

# Nombre del archivo de dependencias en la carpeta de escaneo
REQUIREMENTS_FILE=requirements.txt

# ================================
# CONFIGURACIÓN DE REGLAS DE NEGOCIO
# ================================

# Número de años para considerar un paquete como "mantenido"
# Paquetes publicados en los últimos N años se consideran activos
MAINTAINED_YEARS=2

# Licencias bloqueadas (separadas por comas)
# Licencias copyleft/restrictivas que no son permitidas en el proyecto
# Ejemplos: GPL, LGPL, AGPL
BLOCKED_LICENSES=GPL,GPL-2.0,GPL-3.0,LGPL,LGPL-2.0,LGPL-2.1,LGPL-3.0,AGPL,AGPL-3.0

# ================================
# DEPENDENCY RESOLVER CONFIGURATION
# ================================

# Dependency resolver to use: "uv" (recommended, fast) or "pipgrip" (legacy)
# UV is 10-100x faster than pipgrip with intelligent caching
# Default: uv
DEPENDENCY_RESOLVER=uv

# ================================
# PRIVATE INDEX CONFIGURATION (Optional)
# ================================

# URL of your private PyPI feed (e.g. Azure DevOps Artifacts)
# Example: https://flujodetrabajot.pkgs.visualstudio.com/<org>/_packaging/<feed>/pypi/simple/
PRIVATE_INDEX_URL=

# Personal Access Token (PAT) for authenticating against the private feed
# Azure DevOps: generate at https://dev.azure.com/<org>/_usersSettings/tokens
# Required scopes: Packaging > Read
PRIVATE_INDEX_PAT=

# ================================
# GITHUB AUTHENTICATION (Optional but highly recommended)
# ================================

# GitHub Personal Access Token to increase API rate limit from 60 to 5000 requests/hour
# Without a token, scans of large dependency trees will hit the rate limit quickly.
# Create at: https://github.com/settings/tokens
# Required scopes: none (for public repos) or repo > read (for private repos)
GITHUB_TOKEN=